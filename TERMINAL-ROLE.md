# TERMINAL ROLE ANCHOR — Terminal A (THE FACTORY)
This working tree belongs to **Terminal A**.
ALLOWED: only the factory itself (spec-build, agents, gates, config, quality-core, ui-sync-core, CANON) — improve/harden/upgrade.
FORBIDDEN: producing BANXE product code (Central's job); legacy refactor (B's job).
FIRST ACTION each session: read this file, confirm role in one line, then work.
