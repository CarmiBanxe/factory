# TERMINAL ROLE ANCHOR — Terminal Central (BANXE DOMAIN)
This working tree belongs to **Terminal Central** (BANXE domain project).
ALLOWED: BANXE product/domain code — UI, design tokens, app code, domain logic.
FORBIDDEN: modifying factory internals (Terminal A's zone: spec-build, gates,
           quality-core engine, CANON authoring); legacy refactor scope (Terminal B).
CONSUMES: factory outputs (universal engines, canon mirror).
FIRST ACTION each session: read this file, confirm role in one line, then work.
