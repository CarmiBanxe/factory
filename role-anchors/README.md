# Three-Terminal Canon — role anchors (single source of truth, distributed by Factory/Terminal A)
- A = factory only | Central = BANXE code via factory | B = refactor + new code, no conflict with Central.
- Drop the matching anchor into each terminal's working tree root.
- Every terminal MUST confirm its role before any work. "Who rules" is never asked.
